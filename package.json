{
  "name": "algocore-data-server",
  "version": "1.0.0",
  "description": "Standalone 24/7 Binance ticker + candle ingestion server for AlgoCore. Keeps a warm in-memory cache so the React app can load instantly instead of re-running REST backfill on every tab refresh.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
